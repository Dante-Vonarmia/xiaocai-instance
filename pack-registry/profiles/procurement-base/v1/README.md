# procurement-base v1

该目录表示标准采购领域 profile 的版本化落点。

当前入口：
- `pack-registry/manifests/base-profiles.json` 指向 `domain-packs/*`。
- 本目录用于 profile 版本化落点与说明。

约束：
1. 不写入 tenant 私有数据。
2. 不复制 tenant 专属完整 pack。
