# devlib-only compose
# Development-only overlay. Baseline kernel now lives in compose.instance.yml.

services: {}

networks:
  inst-xiaocai-net:
    name: ${INSTANCE_NETWORK_NAME:-inst-xiaocai-net-dev}
    driver: bridge
