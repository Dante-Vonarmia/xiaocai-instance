# xiaocai Frame Web 环境变量示例
# 使用时复制为 .env 并填写实际值

# API 基础 URL
# 开发环境可以使用 vite proxy，设置为 /api
# 生产环境设置为完整 URL
VITE_API_BASE_URL=/api

# 默认项目 ID（用于会话绑定/设置页）
VITE_DEFAULT_PROJECT_ID=project-default

# core 入口 URL（可选；默认同源 /core/）
# VITE_FLARE_CORE_ENTRY_URL=/core/
